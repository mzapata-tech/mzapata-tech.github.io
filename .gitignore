# Secret keys — never upload these
.env
.env.local
config.js
secrets.js
credentials.json

# Mac/Windows system files (junk)
.DS_Store
Thumbs.db

# Node packages (huge, not needed on GitHub)
node_modules/